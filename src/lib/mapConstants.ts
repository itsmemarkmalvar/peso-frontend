/**
 * Shared map constants. Use these instead of hardcoding coordinates so the app
 * does not assume a specific office location when no geofence data is available.
 */

/** Default map center when no locations or user position exist (Philippines). */
export const DEFAULT_MAP_CENTER: [number, number] = [12.8797, 121.774];

/** Default zoom when showing the default center (country-level). */
export const DEFAULT_MAP_ZOOM = 6;
